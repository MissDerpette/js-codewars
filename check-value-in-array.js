// You will be given an array a and a value x. All you need to do is check whether the provided array contains the value.

// Array can contain numbers or strings. X can be either.

// Return true if the array contains the value, false if not.å

function check(a, x) {
    console.log("a",a);
    console.log("x", x);
    return index = a.includes(x);
   }

// How to Debug
// 1 check value of param 
// 2 check which data type